```bash
systemctl status postgresql
systemctl start postgresql
systemctl enable postgresql
systemctl enable --now postgresql
msfdb init
msfconsole
---
db_status
db_nmap -n -Pn -vv -sT <Network> --reason
hosts
db_nmap -n -Pn -vv -sT -sV -O <Network> --reason
services
vulns
analyze
help database
systemctl reboot
```
